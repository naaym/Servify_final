stages:
  - compile
  - test
  - package
  - docker


  

# ======================
# COMPILE
# ======================
compile-job:
  stage: compile
  image: maven:3.9.6-eclipse-temurin-17
  script:
    - mvn -f backend/pom.xml -q -B clean compile

# ======================
# TEST
# ======================
test-job:
  stage: test
  image: maven:3.9.6-eclipse-temurin-17
  script:
    - mvn -f backend/pom.xml -q -B test

# ======================
# PACKAGE
# ======================
package-job:
  stage: package
  image: maven:3.9.6-eclipse-temurin-17
  script:
    - mvn -f backend/pom.xml -q -B package -DskipTests
  artifacts:
    paths:
      - backend/target/*.jar

# ======================
# DOCKER
# ======================
docker-job:
  stage: docker
  image: docker:latest
  services:
    - docker:dind
  variables:
    DOCKER_TLS_CERTDIR: ""
  script:
    - cp backend/target/*.jar backend/app.jar
    - docker build -t $DOCKERHUB_USERNAME/servify-backend:latest backend
    - docker login -u "$DOCKERHUB_USERNAME" -p "$DOCKERHUB_PASSWORD"
    - docker push $DOCKERHUB_USERNAME/servify-backend:latest
  dependencies:
    - package-job
